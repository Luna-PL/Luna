struct Pair {
    left: i32;
    right: i32;
}

fn main() -> i32 {
    let pair = new Pair(1, 2);
    let left = borrow pair.left;
    let read = fn() -> i32 {
        return left;
    };
    return read();
}

meta version {
    major: i32;
}

fn choice(view: declaration_view, major: i32) -> declaration_ref {
    return select_unique(view, version(major));
}

runtime@version(1)
fn answer() -> i32 { return 1; }

fn main() -> i32 {
    let selected = dynamic select answer with choice(1);
    return selected();
}

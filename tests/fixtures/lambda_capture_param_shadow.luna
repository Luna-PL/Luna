fn main() -> i32 {
    let captured = 5;
    let outer = fn(captured: i32) -> i32 {
        let inner = fn() -> i32 {
            return captured + 1;
        };
        return inner();
    };
    return outer(41);
}

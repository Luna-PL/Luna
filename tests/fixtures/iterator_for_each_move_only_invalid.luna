struct Resource {
    marker: i32;
}

impl Drop for Resource {
    fn drop(resource: &mut Resource) -> unit {}
}

fn main() -> i32 {
    let resource = new Resource(1);
    let values = [move resource];
    values.into_iter().for_each(
        fn(value: Resource) -> unit {});
    return 0;
}

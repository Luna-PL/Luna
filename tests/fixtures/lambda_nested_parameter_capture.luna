fn main() -> i32 {
    let captured = 5;
    let outer = fn(value: i32) -> i32 {
        let inner = fn() -> i32 {
            return value + captured;
        };
        return inner();
    };
    return outer(4);
}

slot context hook(flag: bool);

context invalid(flag: bool) for hook {
    if flag {
        resume();
    } else {
        print(1);
    }
}

fn main() -> i32 {
    apply invalid { hook(true) { } }
    return 0;
}

// A branch that returns after consuming the resource does not need to agree
// with the fall-through state.  The only continuing path consumes it below.
fn main() -> i32 {
    linear let buffer = new i32(1);
    if true {
        free buffer;
        return 0;
    }
    free buffer;
    return 0;
}

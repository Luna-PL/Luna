fn source() -> Result<i32, i32> {
    return Ok(1);
}

slot context hook();

context invalid_propagation for hook {
    let value = source()?;
    resume();
}

fn main() -> i32 {
    apply invalid_propagation {
        hook() {
            print(1);
        }
    }
    return 0;
}

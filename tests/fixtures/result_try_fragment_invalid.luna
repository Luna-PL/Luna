fn source() -> Result<i32, i32> {
    return Ok(1);
}

context invalid_propagation {
    let value = source()?;
    resume();
}

fn main() -> i32 {
    apply hook(invalid_propagation) {
        slot context hook {
            print(1);
        }
    }
    return 0;
}

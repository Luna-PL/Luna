package org.luna.sys;
module prelude;

// Raw Runtime ABI declarations enter this package only on the explicit 0.3
// work line. Sys is a standard-library implementation boundary, not a prelude
// or a convenient user-facing API.
